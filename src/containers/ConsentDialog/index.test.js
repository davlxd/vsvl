import React from 'react'
import ReactDOM from 'react-dom'
import { mount } from 'enzyme'

import Button from '@material-ui/core/Button'

import ConsentDialog from './index'


describe('<SignUp />', () => {
  it('Dialog will not show if user has consent before', () => {
  })

  it('PROCEED button only available when user check both checkboxes', () => {
  })

  it('Dialog will disappear and LocalStorage item stored when user consent', () => {
  })
})
